/**
 * Each
 *
 * @since 1.0.1
 * @category Seq
 * @param {any} objectValue The second number in an addition.
 * @param {any} func The second number in an addition.
 * @returns {null} Returns the total.
 * @example
 *
 * each([1,2],(key,value)=>{
 *
 * })
 *
 */
export function each(objectValue: any, func: any): null;
