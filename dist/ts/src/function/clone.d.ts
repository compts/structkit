/**
 * Array Clone
 *
 * @since 1.0.1
 * @category Seq
 * @param {any} objectValue The second number in an addition.
 * @returns {number} Returns the total.
 * @example
 *
 * clone([1,2])
 * // => [1,2]
 */
export function clone(objectValue: any): number;
