/**
 * Array Concat
 *
 * @since 1.0.1
 * @category Seq
 * @param {any} arrayObject The second number in an addition.
 * @param {any} arrayValue The second number in an addition.
 * @returns {any} Returns the total.
 * @example
 *
 * arrayConcat([1], 2)
 * // => [1,2]
 */
export function arrayConcat(arrayObject: any, arrayValue: any): any;
