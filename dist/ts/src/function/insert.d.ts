/**
 * Insert Value
 *
 * @since 1.0.1
 * @category Seq
 * @param {any} objectValue The first number in an addition.
 * @param {any} value The first number in an addition.
 * @returns {any} Returns the total.
 * @example
 *
 * insert({'as':1}, 'as','as2')
 * // => 1
 */
export function insert(objectValue: any, value: any): any;
