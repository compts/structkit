/**
 * Get JSON Variable
 *
 * @since 1.0.1
 * @category Seq
 * @param {any} objectValue The second number in an addition.
 * @returns {string} Returns the total.
 * @example
 *
 * getTypeof([])
 * => array
 */
export function getTypeof(objectValue: any): string;
