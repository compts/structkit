export default has;
/**
 * Check if object has value
 *
 * @since 1.0.1
 * @category Seq
 * @param {any} value JSON or Array
 * @param {any} key For key or index of data
 * @returns {boolean} Returns true or false.
 * @example
 *
 * has({'as':1}, 'as')
 * // => true
 */
declare function has(value: any, key: any): boolean;
